## 🔗 관련 이슈
Closes #33

## ✅ 작업 내용

### 배경 — Spring의 OCR 트리거 발행 구현으로 `OcrJob` 계약이 바뀌었습니다

Spring 백엔드가 OCR 트리거(`ocr-job-queue`) 발행을 구현하면서 `OcrJob` 메시지 계약이 변경됐습니다. 발행측(정본)이 먼저 나갔고, 소비측인 이 레포의 `core.contracts`가 뒤처져 **계약 불일치** 상태였습니다.

핵심은 단순 필드 추가가 아니라 **소유권 이동**입니다. 이제 Spring이 `reports`·`report_attachments` shell 행을 먼저 생성(pending)하고, 워커는 OCR·AI 결과로 **그 행을 UPDATE**(생성이 아니라)합니다. 그래서 UPDATE 대상 참조 키와 fan-in 판별용 메타를 메시지에 실어 보냅니다.

- 발행측 정본: `infra/kafka/OcrJob.java`, `report/service/ReportCommandService.java` (backend `d09e0e6`, `cbcf85c`)
- 발행 방식: Java `record`(camelCase) → 전역 Jackson `SNAKE_CASE` → snake_case JSON

<br/>

### 1. `OcrJob` 계약에 필드 4개 추가

소비측 pydantic 계약(`core.contracts.OcrJob`)을 발행측 정본에 맞췄습니다.

| 필드 | 타입 | 역할 |
|---|---|---|
| `report_id` | string(UUID), **필수** | `REPORTS.id` — 결과 UPDATE 대상(Spring이 shell 생성) |
| `attachment_id` | string(UUID), **필수** | `REPORT_ATTACHMENTS.id` — 결과 UPDATE 대상 |
| `doc_index` | int \| null | 청구 내 문서 순번(1-based) |
| `doc_total` | int \| null | 청구 총 문서 수 — 워커의 fan-in(리포트 생성) 판별용 |

```python
class OcrJob(BaseModel):
    ...
    claim_id: str | None = None
    report_id: str            # 🆕 REPORTS.id — 결과 UPDATE 대상
    attachment_id: str        # 🆕 REPORT_ATTACHMENTS.id — 결과 UPDATE 대상
    doc_index: int | None = None   # 🆕 청구 내 문서 순번(1-based)
    doc_total: int | None = None   # 🆕 청구 총 문서 수 — fan-in 판별
    uploaded_at: datetime
```

#### 세부
- `core.contracts.OcrJob` → 필드 추가 + docstring에 소유권/fan-in 설명
- `.claude/docs/contracts.md §1` → 필드 표·JSON 예시·소유권/fan-in 노트 갱신(단일 출처 정합)

<br/>

### 2. 계약을 테스트로 고정

경계 계약(Kafka 메시지 스키마)은 반드시 테스트로 고정한다는 컨벤션(§12)에 따라 케이스를 추가했습니다.

- 신규 필드 포함 계약 예시 라운드트립 검증
- `report_id`·`attachment_id` 누락 시 `ValidationError` — 진입점에서 차단(→ DLQ)
- `doc_index`·`doc_total`은 옵셔널(누락 시 `None`)
- 기존 파이프라인·e2e `_job` 팩토리에 신규 필수 필드를 반영해 회귀 방지

## 💬 고민했던 부분

### `report_id`·`attachment_id`를 required로

발행측이 두 값을 **항상 non-null**로 채우고, UPDATE 대상 행을 특정하지 못하는 메시지는 애초에 처리가 불가능합니다. optional로 두면 실패를 뒤로 미룰 뿐이라, 계약이 발행측의 보장을 그대로 인코딩하도록 **required(`str`)** 로 했습니다. 반면 `doc_index`·`doc_total`은 발행측 Java 타입이 `Integer`(nullable)라 `int | None`으로 맞췄습니다.

### 이번 PR 범위를 계약·검증으로 한정

워커의 `_publish_report`는 현재 `ocr_result_id`에서 `report_id`를 **자체 파생**합니다. 새 계약에선 `job.report_id`(Spring 생성)를 패스스루하고, 문서를 1건씩 발행하므로 **fan-in**으로 마지막 문서에서만 `ReportJob`을 발행하도록 게이팅해야 합니다. 이건 상태 저장소(DB 카운터 vs Redis)·멱등 처리 등 **설계 결정이 필요**한 부분이라, 이번 PR은 계약(필드) 추가·검증까지만 하고 파이프라인 로직 전환과 `ReportJob`(§2) 의미 정정은 **후속으로 분리**했습니다. 현행 파이프라인 동작은 불변이라 회귀가 없습니다(유닛 118 passed).

### (참고) 발행측 확인 중 드러난 연관 드리프트

이번 PR 범위 밖이지만 별도 정렬이 필요한 항목을 이슈 #33에 기록해뒀습니다 — 실제 `ocr_results` 스키마의 컬럼 4개 추가(§3), 정본에 `report_drafts` 테이블 부재(§4, `reports`+`report_issues`+`report_reviews`로 대체), 발행측 `toContentType()`/`toS3Key()`의 null 반환 가능성.
